# Tutorial 02 — Pure Functions: What They Really Are (And What Breaks Them)

> **flow-arch / flow-starter**
> Tutorial-00 introduced pure functions as a concept.
> This tutorial goes deeper — because the most common mistakes
> come from code that _looks_ pure but isn't.

---

## The definition, precisely

A function is **pure** if and only if it satisfies **both** rules simultaneously:

> **Rule 1 — Deterministic:** Given the same input, it always returns the same output.
> **Rule 2 — No side effects:** It does not touch anything outside itself.

Both rules must hold. One without the other is not enough.

```javascript
// ✅ Pure — both rules satisfied
const double = (n) => n * 2;
double(4); // → 8, always, forever

// ❌ Breaks Rule 1 — same input, different output possible
let multiplier = 2;
const scale = (n) => n * multiplier;
scale(4); // → 8 now
multiplier = 3;
scale(4); // → 12 now — same input, different output

// ❌ Breaks Rule 2 — modifies something outside itself
const log = [];
const trackDouble = (n) => {
  log.push(n); // side effect: modifying external array
  return n * 2;
};
```

---

## 1. The three ways a function becomes impure

### Way 1 — Closure dependency on external variables

This is the most common mistake in declarative-style code.

```javascript
// This looks declarative and clean
const result = items
  .map((x) => x.values)
  .reduce((a, b) => a.concat(b), [])
  .map((v) => v * factor) // ← the problem is here
  .reduce((sum, v) => sum + v, 0);
```

The `.map(v => v * factor)` callback closes over `factor`.
If `factor` lives outside this function and can change, the entire chain is **impure**.

```javascript
// Scenario that breaks purity
let factor = 2;

const processItems = (items) =>
  items
    .flatMap((x) => x.values)
    .map((v) => v * factor) // depends on external factor
    .reduce((sum, v) => sum + v, 0);

processItems(data); // → 100 (factor = 2)
factor = 3;
processItems(data); // → 150 — same input, different output
// ❌ Not deterministic — Rule 1 broken
```

**The fix — make every dependency explicit:**

```javascript
// ✅ Pure — factor is now a parameter
const processItems = (items, factor) =>
  items
    .flatMap((x) => x.values)
    .map((v) => v * factor)
    .reduce((sum, v) => sum + v, 0);

processItems(data, 2); // → 100, always
processItems(data, 2); // → 100, always — deterministic
```

**The rule:**

> If a function uses a variable it did not receive as a parameter,
> it is not pure — unless that variable is a primitive `const` that can never change.

```javascript
// ✅ Fine — PI is a primitive constant, truly immutable
const PI = 3.14159;
const circleArea = (r) => PI * r * r;

// ❌ Not fine — const object, but its contents can still mutate
const config = { factor: 2 };
const scale = (n) => n * config.factor;
config.factor = 3; // this works — config is mutated
scale(4); // → 12 now, not 8
```

---

### Way 2 — Hidden side effects inside callbacks

`.map()` and `.reduce()` do not mutate the original array.
But your **callback** can still cause side effects.

**Mutating object properties:**

```javascript
const items = [
  { name: "a", price: 10 },
  { name: "b", price: 20 },
];

// ❌ Looks like a transformation — is actually mutation
const result = items.map((item) => {
  item.price = item.price * 2; // modifying the original object
  return item;
});

console.log(items[0].price); // → 20 — the original was changed
// Rule 2 broken — modified something outside the function
```

**The fix — always return a new object:**

```javascript
// ✅ Pure — creates a new object, never touches the original
const result = items.map((item) => ({
  ...item, // copy all existing properties
  price: item.price * 2, // override only what changes
}));

console.log(items[0].price); // → 10 — original unchanged
```

**Logging inside transformation:**

```javascript
// ❌ Impure — console.log is a side effect
const result = items.map((item) => {
  console.log("processing:", item); // side effect
  return item.value * 2;
});

// ✅ Pure — no side effects inside the transformation
const result = items.map((item) => item.value * 2);

// If you need to debug, do it at the boundary
console.log("input:", items);
const result = items.map((item) => item.value * 2);
console.log("output:", result);
```

**Modifying shared state:**

```javascript
// ❌ Impure — modifies external counter from inside a transformation
let totalProcessed = 0;

const view = (items) =>
  items.map((item) => {
    totalProcessed++; // side effect
    return `<li>${item.name}</li>`;
  });

// ✅ Pure — view only transforms data to HTML
const toListItem = (item) => `<li>${item.name}</li>`;
const view = (items) => items.map(toListItem).join("");

// Counting happens at the boundary, not inside the transformation
```

---

### Way 3 — Reading from global or mutable data sources

```javascript
// ❌ Impure — reads from global state directly
const getActiveItems = () =>
  window.appState.items.filter((item) => item.active);
// Same call at different times returns different results
// depending on what window.appState contains at that moment

// ✅ Pure — receives state as a parameter
const getActiveItems = (items) => items.filter((item) => item.active);

// The caller controls what data goes in
const active = getActiveItems(state.items);
```

In flow-arch, this is the entire point of passing `state` through the reducer:

```javascript
// ✅ Pure reducer — state comes in as a parameter, never read from outside
const reducer = (state, action) => {
  switch (action.type) {
    case "FILTER_ACTIVE":
      return {
        ...state,
        items: state.items.filter((item) => item.active),
      };
    default:
      return state;
  }
};
// Same state + same action → same new state, always
```

---

## 2. Pure vs Impure — the full comparison

| Property           | Pure Function                    | Impure Function                                     |
| ------------------ | -------------------------------- | --------------------------------------------------- |
| **Predictability** | Same input → same output, always | Output may vary with external state                 |
| **Dependencies**   | Only its parameters              | External variables, global state, closures          |
| **Side effects**   | None                             | May modify globals, log, mutate objects             |
| **Testability**    | One line per test, zero setup    | Requires mocking external environment               |
| **Composability**  | Chain safely in any order        | Order-dependent, unpredictable when combined        |
| **Debuggability**  | Only the function itself matters | Must trace entire execution context                 |
| **Cacheability**   | Safe to memoize                  | Cannot cache — same input may need different output |

---

## 3. Why purity matters so much in flow-arch

### Testability — zero setup required

```javascript
// Pure function — testing is trivial
const isActive = (item) => item.active === true;

expect(isActive({ active: true })).toBe(true); // one line
expect(isActive({ active: false })).toBe(false); // one line
// No setup. No mocks. No teardown.

// Impure function — testing is painful
const getActiveFromGlobal = () => window.appState.items.filter((i) => i.active);

// Must construct the entire global environment first
window.appState = { items: [{ active: true }, { active: false }] };
expect(getActiveFromGlobal()).toEqual([{ active: true }]);
// Must clean up — or tests interfere with each other
delete window.appState;
```

### Composability — safe to chain

```javascript
// Because each function is pure, you can compose them freely
// Inserting, removing, or reordering steps cannot break other steps

const processItems = (items, factor) =>
  items
    .filter(isActive) // pure — safe to remove or reorder
    .map(toValue) // pure — safe to remove or reorder
    .map((v) => v * factor) // pure — factor is a parameter
    .reduce(sum, 0); // pure — safe to remove or reorder
```

### Memoization — safe caching

```javascript
// Pure functions are deterministic — the same input always gives the same output
// This makes it safe to cache results

const memoize = (fn) => {
  const cache = new Map();
  return (...args) => {
    const key = JSON.stringify(args);
    if (cache.has(key)) return cache.get(key);
    const result = fn(...args);
    cache.set(key, result);
    return result;
  };
};

// Safe to memoize because it's pure
const expensiveTransform = memoize((items, factor) =>
  items
    .flatMap((x) => x.values)
    .map((v) => v * factor)
    .reduce((sum, v) => sum + v, 0),
);

expensiveTransform(data, 2); // computed — takes 50ms
expensiveTransform(data, 2); // from cache — takes 0ms
```

### Parallelism — no race conditions

```javascript
// Pure functions share no state
// Two pure functions can run simultaneously without interfering

const [result1, result2] = await Promise.all([
  processItems(dataset1, factor), // pure
  processItems(dataset2, factor), // pure
]);
// No race condition — they operate on separate data, share nothing

// This is also the foundation of safe concurrency in Haskell and Elixir —
// two of the languages flow-arch explores for backend pipelines
```

---

## 4. The `const` misconception

The most common misunderstanding:

> "`const` means immutable — so closing over a `const` is safe."

**This is only half true.**

```javascript
// ✅ const primitive — truly immutable, safe to close over
const PI = 3.14159;
const TAX_RATE = 0.08;
const APP_NAME = "flow-arch";
// Numbers, strings, booleans — const makes them truly constant

// ❌ const object — the reference is constant, the contents are not
const config = { factor: 2 };
config.factor = 3; // ✅ this works — const does not deep-freeze objects
config = {}; // ❌ this fails — can't reassign the reference

// ❌ const array — same problem
const items = [1, 2, 3];
items.push(4); // ✅ this works — the array is mutated
items = []; // ❌ this fails — can't reassign the reference
```

**How to make objects truly immutable:**

```javascript
// Object.freeze — prevents property modification (shallow)
const config = Object.freeze({ factor: 2 });
config.factor = 3; // silently fails, or throws in strict mode
console.log(config.factor); // → 2 — unchanged

// For nested objects, freeze does not go deep
const state = Object.freeze({
  user: { name: "Austin" }, // this nested object is NOT frozen
});
state.user.name = "Bob"; // this works — shallow freeze only
```

> **Rule:** `const` prevents reassignment of the variable.
> It does not prevent mutation of the value it points to.
> Only primitive values are truly immutable with `const`.
> Use `Object.freeze()` for objects — but know it is shallow.

---

## 5. The flow-arch pattern — making everything explicit

The practical rule in flow-arch:

> **Every dependency a function needs must be passed as a parameter.**
> **No hidden dependencies. No global reads. No closure over mutable values.**

```javascript
// ❌ flow-arch violation — implicit dependencies
const state = { items: [], factor: 2 };

const view = () =>
  // no parameters — reads from closure
  state.items
    .filter((item) => item.active)
    .map((item) => `<li>${item.name}</li>`)
    .join("");

// ✅ flow-arch style — all dependencies are parameters
const isActive = (item) => item.active;
const toListItem = (item) => `<li>${item.name}</li>`;

const view = (
  state, // state passed in explicitly
) => state.items.filter(isActive).map(toListItem).join("");
```

### The complete flow-arch data pipeline

```javascript
// ── 1. Pure transformation functions ────────────────────────────

const isActive = (item) => item.active;
const toValue = (item) => item.value;
const multiply = (factor) => (v) => v * factor; // curried pure function
const sum = (acc, v) => acc + v;
const toListItem = (item) => `<li>${item.name}</li>`;

// ── 2. Pure calculation (composable, testable, memoizable) ──────

const calculateTotal = (items, factor) =>
  items.filter(isActive).map(toValue).map(multiply(factor)).reduce(sum, 0);

// ── 3. Pure view function ────────────────────────────────────────

const view = (state) => `
  <div class="summary">
    <p>Total: ${calculateTotal(state.items, state.factor)}</p>
    <ul>${state.items.filter(isActive).map(toListItem).join("")}</ul>
  </div>
`;

// ── 4. Pure reducer ──────────────────────────────────────────────

const reducer = (state, action) => {
  switch (action.type) {
    case "SET_FACTOR":
      return { ...state, factor: action.payload };
    case "TOGGLE_ITEM":
      return {
        ...state,
        items: state.items.map((item) =>
          item.id === action.payload ? { ...item, active: !item.active } : item,
        ),
      };
    default:
      return state;
  }
};

// ── 5. Boundary — side effects contained here only ───────────────

class FlowComponent extends HTMLElement {
  constructor() {
    super();
    this.attachShadow({ mode: "open" });
    this._state = { items: [], factor: 1 };
  }

  dispatch(action) {
    this._state = reducer(this._state, action); // pure — new state
    this.shadowRoot.innerHTML = view(this._state); // side effect — DOM write
  }

  connectedCallback() {
    this.shadowRoot.innerHTML = view(this._state); // side effect — DOM write
  }
}

// Everything above dispatch() is pure.
// DOM mutation happens only in dispatch() and connectedCallback().
// The line between pure and impure is explicit and enforced by structure.
```

---

## Summary

| Concept                   | What to remember                                                                   |
| ------------------------- | ---------------------------------------------------------------------------------- |
| **Pure function**         | Deterministic + no side effects — both rules, always                               |
| **Closure trap**          | Closing over a variable = impure, unless it's a primitive `const`                  |
| **Object mutation**       | Always `{ ...item, prop: newValue }` — never `item.prop = value`                   |
| **Global reads**          | Pass state as a parameter — never read from `window` or globals inside a transform |
| **`const` misconception** | `const` prevents reassignment, not mutation of objects/arrays                      |
| **Testability**           | Pure functions need zero setup — one assertion per test                            |
| **Memoization**           | Only safe for pure functions — impure functions cannot be cached                   |
| **flow-arch rule**        | All dependencies must be parameters — no hidden state, no surprises                |

---

## What's next

- [Tutorial 03 — Lazy evaluation: why `.map()` is eager and what to do about it](./tutorial-03.md)
- [Tutorial 04 — Eager vs Lazy: What They Are, Every Lazy Pattern, and Why flow-arch Cares](./T-04.md)
- [Back to Tutorial 01 — Why `for` / `while` make your brain work harder](./tutorial-01.md)
- [See the live demos](../index.html)

---

_flow-arch / flow-starter · tutorial-02_
_This document is part of the flow-arch open exploration project._
_Contributions welcome — see [CONTRIBUTING.md](../CONTRIBUTING.md)_
